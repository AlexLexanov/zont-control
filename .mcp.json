{
  "mcpServers": {
    "zont-control": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.mjs"],
      "env": {
        "ZONT_TOKEN": "${ZONT_TOKEN}",
        "ZONT_CLIENT_EMAIL": "${ZONT_CLIENT_EMAIL}"
      }
    }
  }
}
